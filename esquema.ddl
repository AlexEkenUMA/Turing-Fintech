CREATE TABLE CUENTA (IBAN VARCHAR NOT NULL, DTYPE VARCHAR(31), SWIFT VARCHAR, ESTADO BOOLEAN NOT NULL, FECHA_APERTURA DATE NOT NULL, SALDO DOUBLE, TIPO VARCHAR NOT NULL, COMISION DOUBLE, NOMBRE_BANCO VARCHAR, PAIS VARCHAR, SUCURSAL VARCHAR, PRIMARY KEY (IBAN))
CREATE TABLE CLIENTE (ID BIGINT NOT NULL, DTYPE VARCHAR(31), CIUDAD VARCHAR NOT NULL, CODIGO_POSTAL INTEGER, DIRECCION VARCHAR NOT NULL, ESTADO VARCHAR NOT NULL, FECHA_ALTA DATE, FECHA_BAJA DATE, PAIS VARCHAR NOT NULL, TIPO_CLIENTE VARCHAR NOT NULL, APELLIDOS VARCHAR NOT NULL, FECHA_NACIMIENTO DATE, NOMBRE VARCHAR NOT NULL, RAZON_SOCIAL VARCHAR NOT NULL, PRIMARY KEY (ID))
CREATE TABLE USUARIO (NOMBRE_USUARIO VARCHAR NOT NULL, ADMINISTRATIVO BOOLEAN, CONTRASEÑA VARCHAR, PRIMARY KEY (NOMBRE_USUARIO))
CREATE TABLE DIVISAS (ABREVIATURA VARCHAR NOT NULL, CAMBIO_EURO DOUBLE, NOMBRE VARCHAR, SIMBOLO CHAR, PRIMARY KEY (ABREVIATURA))
CREATE TABLE TRANSACCIONES (ID_UNICO BIGINT NOT NULL, CANTIDAD DOUBLE, COMISION DOUBLE, CONCEPTO VARCHAR, FECHA_INSTRUCCION TIMESTAMP, NOMBRE_EMISOR VARCHAR, TIPO_TRANSACCION VARCHAR, PRIMARY KEY (ID_UNICO))
CREATE TABLE AUTORIZADO (IDENTIFICACION BIGINT NOT NULL, APELLIDOS VARCHAR NOT NULL, DIRECCION VARCHAR NOT NULL, ESTADO VARCHAR, FECHA_FIN DATE, FECHA_INICIO DATE, FECHA_NACIMIENTO DATE, NOMBRE VARCHAR NOT NULL, PRIMARY KEY (IDENTIFICACION))
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT NUMERIC(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
